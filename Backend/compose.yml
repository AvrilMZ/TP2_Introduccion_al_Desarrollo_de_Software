services:
  viaje:
    image: postgres:17
    container_name: database
    environment:
      - POSTGRESS_USER = Facu
      - POSTGRESS_PASSWORD = 1234
      - POSTGRESS_DB = viajando
    ports:
      - 5432:5432
    volumes:
      - ./data:/var/lib/postgresql/data